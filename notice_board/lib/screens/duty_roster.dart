import 'package:flutter/material.dart';

class MyDutyRoster extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "...to be implemented soon!",
          style: TextStyle(color: Colors.red, fontSize: 20.0),
        ),
      ),
      color: Colors.white12,
    );
  }
}
